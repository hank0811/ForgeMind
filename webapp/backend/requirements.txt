# Website interface backend only -- the core engine (engine/pyproject.toml)
# stays dependency-light with just PyYAML. Install both to run the website:
#   pip install -e ./engine
#   pip install -r webapp/backend/requirements.txt
Flask>=3.0
# Production WSGI server (Render deployment). Not used for local dev --
# `python webapp/backend/app.py` uses Flask's own dev server. The gunicorn
# package installs fine on Windows; only running it requires Linux.
gunicorn>=23.0
# Persistent task storage (storage.py). Only used when DATABASE_URL is set
# (e.g. on Render); local dev and the test suite run with it unset, in
# which case storage.py's functions are all no-ops and this is never
# imported.
psycopg[binary]>=3.1
